import React from "react";
import { Grid } from "@material-ui/core";

function Work() {
    return (
        <Grid container style={{ justifyContent: "center" }}>
            temp
        </Grid>
    );
}
export default Work;
